---
import Layout from '$layouts/Layout.astro';
import ExploreBtn from '$components/ExploreButton.astro';
import { generateUrl } from '$utils/navigation';
---

<Layout title="Frontend Mentor | Space tourism website">
  <main class="grid-container">
    <div class="presentation space-y">
      <h1 class="uppercase space-y">
        <span class="fs-md ff-condensed ls-widest">So, you want to travel to</span>
        <span class="fs-3xl ff-serif ls-normal text-light">Space</span>
      </h1>
      <p>
        Let’s face it; if you want to go to space, you might as well genuinely go to outer space and not hover kind of
        on the edge of it. Well sit back, and relax because we’ll give you a truly out of this world experience!
      </p>
    </div>
    <div class="call-to-action">
      <ExploreBtn href={generateUrl('destination', 'moon')}>Explore</ExploreBtn>
    </div>
  </main>
</Layout>

<style>
  .grid-container {
    --container-padding: calc(var(--gutters-block) * 1.5) var(--gutters-inline) calc(var(--gutters-block) * 2);
    --container-auto-rows: auto;
    place-items: center;
    row-gap: clamp(2rem, calc(-0.56rem + 10.94vw), 6rem);
  }
  .presentation h1 > * {
    display: block;
  }

  @media (--screen-desktop) {
    .grid-container {
      place-items: end;
      padding-bottom: var(--gutters-bottom-clamp) !important;
    }
    .presentation {
      justify-self: start;
    }
  }
</style>

<style is:global>
  body {
    background-image: url('/assets/home/background-home-mobile.jpg');
    background-position: bottom;
  }
  @media (--screen-tablet) {
    body {
      background-image: url('/assets/home/background-home-tablet.jpg');
    }
  }
  @media (--screen-desktop) {
    body {
      background-image: url('/assets/home/background-home-desktop.jpg');
      background-position: center;
    }
  }
</style>
