<footer class="text-muted">
  <span>
    Challenge by <a href="https://www.frontendmentor.io?ref=challenge" target="_blank">Frontend Mentor</a>.
  </span>
  <span>Coded by <a href="https://github.com/AntoineC-dev" aria-label="Github">Antoine C</a>.</span>
</footer>

<style>
  footer {
    position: absolute;
    bottom: 0;
    left: 0;
    padding: 0.75rem 1.5rem;
    text-align: center;
    font-size: 0.75rem;
    background-color: hsl(var(--clr-dark) / 0.5);
    border-top-right-radius: 0.5rem;
  }

  @supports (backdrop-filter: var(--backdrop-blur)) {
    footer {
      background-color: hsl(var(--clr-light) / 0.04);
      backdrop-filter: var(--backdrop-blur);
    }
  }

  footer > span {
    display: inline-block;
  }

  footer a {
    color: hsl(var(--clr-muted));
    transition: color 150ms linear;
  }

  footer a:hover {
    color: hsl(var(--clr-light));
  }

  @media (--screen-tablet) {
    footer {
      font-size: 0.875rem;
      padding-inline: 2.5rem;
    }
  }
</style>
